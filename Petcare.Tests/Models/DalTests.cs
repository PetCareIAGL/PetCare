using System;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;

[TestClass]
public class DalTests
{
    [TestMethod]
    public void CreateAdvertissement()
    {
        /*using (IDal dal = new Dal())
        {
            dal.CreateAdvertissement(null, "Le tritre", "La description", null);
            List<AdvertissementModel> advertissements = dal.getAllAdvertissements();

            Assert.IsNotNull(advertissements);
            Assert.AreEqual(1, advertissements.Count);
            Assert.AreEqual("Le titre", advertissements[0].title);
            Assert.AreEqual("La description", advertissements[0].description);
        }*/
    }
}